[package]
name = "cerberus-opsct"
version = "0.1.0"
edition = "2026"

[dependencies]
crossterm = {version = "0.28.1", features = ["event-stream"]} 
image = "0.25.5"
viuer = {version = "0.9.1", features = ["print-file"]}
